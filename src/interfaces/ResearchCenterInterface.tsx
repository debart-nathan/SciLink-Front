export default interface ResearchCenterInterface {
  id: string;
  label: string;
  sigle: string;
  founding_year: string;
  is_active: boolean;
  website: string;
  fiche_msr: string;
  users: number[];
}